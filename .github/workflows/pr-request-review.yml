name: Request Review

on:
  issue_comment:
    types:
      - created

jobs:
  request-review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.repository_owner == 'cryostatio' && github.event.issue.pull_request && startsWith(github.event.comment.body, '/request_review')
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Add label and request review
        run: |
          reviewers="@cryostatio/reviewers"

          echo "Adding label 'review-requested' to the PR"
          gh pr edit ${{ github.event.issue.number }} --add-label "review-requested"

          if [ -n "$reviewers" ]; then
            gh pr review ${{ github.event.issue.number }} $reviewers
          else
            echo "Requesting review from @cryostatio/reviewers team"
            gh pr review ${{ github.event.issue.number }} cryostatio/reviewers
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
