query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    doDelete {
                        name
                        state
                    }
                }
            }
        }
    }
}
