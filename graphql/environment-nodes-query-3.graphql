query {
    environmentNodes(filter: { name: "JDP" }) {
        name
        nodeType
        descendantTargets {
            recordings {
                active {
                    name
                    state
                }
                archived {
                    name
                }
            }
        }
    }
}
