query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            deleteRecordings(names: ["bar"]) {
                name
                state
            }
        }
    }
}
