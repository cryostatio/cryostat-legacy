query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            # startRecording(recording: {
            #     name: "foo",
            #     template: "Profiling",
            #     templateType: "TARGET",
            #     duration: 30
            #     }) {
            #         name
            #         state
            # }
            stopRecordings {
                name
                state
            }
            recordings {
                active {
                    name
                    state
                }
            }
        }
    }
}
