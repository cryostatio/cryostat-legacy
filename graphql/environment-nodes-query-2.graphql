query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            startRecording(recording: {
                name: "bar",
                template: "Continuous",
                templateType: "TARGET",
                duration: 0
                }) {
                    name
                    state
            }
        }
    }
}
