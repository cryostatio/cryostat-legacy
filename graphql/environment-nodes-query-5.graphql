query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    stop {
                        name
                        state
                    }
                }
            }
        }
    }
}
