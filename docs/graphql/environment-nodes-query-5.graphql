query {
    environmentNodes(filter: { name: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    data {
                        doStop {
                            name
                            state
                        }
                    }
                }
            }
        }
    }
}
