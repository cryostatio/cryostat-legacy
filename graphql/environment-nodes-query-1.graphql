query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            startRecording(recording: {
                name: "foo",
                template: "Profiling",
                templateType: "TARGET",
                duration: 30
                }) {
                    name
                    state
            }
        }
    }
}
