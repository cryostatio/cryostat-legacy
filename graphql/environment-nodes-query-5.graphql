query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    doStop {
                        name
                        state
                    }
                }
            }
        }
    }
}
