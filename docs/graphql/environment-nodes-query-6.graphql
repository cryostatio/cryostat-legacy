query {
    environmentNodes(filter: { name: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    doDelete {
                        name
                        state
                    }
                }
            }
        }
    }
}
