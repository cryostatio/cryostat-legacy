query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            recordings {
                archived {
                    delete {
                        name
                    }
                }
            }
        }
    }
}
