query {
    environmentNodes(filter: { nodeName: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    delete {
                        name
                        state
                    }
                }
            }
        }
    }
}
