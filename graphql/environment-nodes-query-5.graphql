query {
    environmentNodes(filter: { name: "JDP" }) {
        descendantTargets {
            recordings {
                active {
                    doStop {
                        name
                        state
                    }
                }
            }
        }
    }
}
